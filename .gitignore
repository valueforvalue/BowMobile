# OS files
.DS_Store
Thumbs.db

# Flutter release artifacts (built locally, not committed)
bow_mobile/build/
app-release.apk
app-debug.apk

# Database (must be placed manually before building the APK)
bow_mobile/assets/parts.db
